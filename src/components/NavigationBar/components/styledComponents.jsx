import { styled } from "@mui/system";
import { alpha, Button as MuiButton, InputBase } from "@mui/material";

const Button = styled(MuiButton)(({ theme }) => ({
  textTransform: "none",
  whiteSpace: "nowrap",
}));
{
}
export { Button };
